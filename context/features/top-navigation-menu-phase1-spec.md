# Top navigation menu UI Phase 1 Spec

## Overview

This is phase 1 of 2 for the top navigation UI layout. Use the screenshot referenced below for how it should look.

## Requirements

- Add a primeNG menubar component to the files in @frontend/src/app/shell/header-nav
- The menubar should be created using custom content/templates
- Use the Toggle_menu.png as reference to what the menubar should look like. However, I want the brand name 'ActivSwitzerland' to be on the left and the hamburger toggle to be on the right.
- The activation of the side menu drawer when the toggle button is clicked will be done in phase 2.

## References

- @context/screenshots/Toggle_menu.png
- @context/project-overview.md
